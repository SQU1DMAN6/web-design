{
  "name": "inker-native",
  "version": "1.0.0",
  "private": true,
  "description": "Native WinFsp FUSE3 filesystem addon for Inker — mounts Q:\\ as InkDrop Drive",
  "main": "index.js",
  "scripts": {
    "build": "node-gyp rebuild",
    "install": "node-gyp rebuild",
    "test": "node -e \"const n = require('./build/Release/winfsp_native.node'); console.log('Available:', Object.keys(n)); console.log('isAvailable:', n.isAvailable());\""
  },
  "gypfile": true,
  "dependencies": {
    "node-addon-api": "^8.8.0"
  }
}